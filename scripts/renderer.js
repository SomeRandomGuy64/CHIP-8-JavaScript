class Renderer 
{

    //constructor, takes an argument "scale" which allows me to scale the display
    constructor(scale)
    {
        //display size: 64x32
        this.cols = 64;
        this.rows = 32;
    }
}

export default Renderer;